# 风格提取与提示词生成工作流v12升级记录-2026-05-04

date: 2026-05-04

# 记录时间
2026-05-04

# 用户指令
“C:\Users\kid\Desktop\新建文本文档.txt 这里面都是案例和优秀模板，但并不一定都适用，使用时要理解逻辑，重点根据使用场景判断。理解完后，优化刚才建立的风格提取和提示词生成 skill。”

# 已完成动作
1. 读取并分析文档（共 1195 行，分段读取）。
2. 提炼核心方法：
   - 不机械套模板
   - 先做“使用场景判定”
   - 明确“保持不变项”与“可变项”
   - 输出要可复制、可投喂、可复现
3. 创建新技能：`style-library-v12-scenario-aware`（category: creative）。

# v12 skill 核心升级
- 新增任务分型：
  A 风格收录
  B 套用风格
  C 草图/线稿->效果图
  D 现状照片->设计还原/改造
  E 局部编辑（材质/季节/天气）
  F 实景或渲染->分析图
- 强化双模式：render_mode / lineart_mode
- 强制“保持不变项”约束（结构、透视、轮廓、比例）
- 固化三组变体逻辑：精准还原 / 设计变体 / 氛围叙事变体
- 输出格式固定为：
  1) 场景判定与假设
  2) 提示词代码块
  3) 参数建议与负向词
- 纳入 nanobanana2 参数建议与中英负向词基线

# 本轮约定（长期生效）
- 模板可借鉴但不照搬
- 以场景判断为第一原则
- 继续维护总风格库与命名系统（machine_name + human_name）

# 可调用口令
- 收录风格 <图片>
- 套用风格 <风格名> 渲染图
- 套用风格 <风格名> 线稿图
- 风格迁移 <源图> -> <目标图>
- 局部替换 <区域> <材质>
